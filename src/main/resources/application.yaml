spring:

  profiles:
    active: dev

  datasource:
    hikari:
      max-lifetime: 1200000
      maximum-pool-size: 10
      connection-timeout: 300000
      minimum-idle: 5
    type: com.zaxxer.hikari.HikariDataSource

  redis:
    jedis:
      pool:
        max-idle: 1
        max-active: 8
    database: 0

  application:
    name: blog

mybatis-plus:
  type-aliases-package: com.example.blog.entity

jwt:
  data:
    SECRET: dafok4DADf54a5fa1BD1ahj4u1AA2150DAcasda154i5
    tokenHeader: Authorization
    expiration: 604800 #7days
    tokenHead: Bearer
